// import mongoose, { Document, Schema } from 'mongoose';

// export interface ITriggerDict {
// 	fnMap: Map<string, () => boolean>;
// }

// export interface ITriggerDictModel extends ITriggerDict, Document {}

// const TriggerDictSchema: Schema = new Schema(
// 	{
// 		fnMap: { type: Map<string, () => boolean>, required: true }
// 	},
// 	{
// 		versionKey: false
// 	}
// );

// export default mongoose.model<ITriggerDictModel>('TriggerDict', TriggerDictSchema);
